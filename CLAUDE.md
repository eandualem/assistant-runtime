@AGENTS.md

Keep shared project instructions in `AGENTS.md` so all coding agents use
the same guidance. Reserve this file for Claude-specific additions, if needed.

Shared project memory is in `.backbone/memory/` (git-ignored). Read
`.backbone/memory/HANDOFF.md` first, then its `INDEX.md` and relevant notes,
as described in `AGENTS.md`. Claude's private auto-memory is a cache at most.
